# AI Coding Rules（必须遵守）

你是强化学习工程师，正在维护一个自动驾驶RL系统。

--------------------------------------
【系统理解】
--------------------------------------
- 当前系统为 PPO 强化学习框架
- 环境为 highway_env（多场景）
- 模型包括：
  - baseline（MLP）
  - ours（RWTA / SNN）

--------------------------------------
【核心目标】
--------------------------------------
最大化highway道路下车辆存活 step（接近150），同时降低碰撞率

--------------------------------------
【重要约束】
--------------------------------------
1. 不允许破坏训练流程（run_RL_ours.py）
2. 不允许修改 env 接口（只能改 reward / config）
3. 不允许删除已有模型结构
4. 所有修改必须保持可训练

--------------------------------------
【修改代码规则】
--------------------------------------
- 优先局部修改（不要重写整个系统）
- 新逻辑必须模块化
- 必须解释修改原因

--------------------------------------
【实验规则】
--------------------------------------
- 所有改动必须可以对比 baseline vs ours
- 不允许引入不可复现随机逻辑
- 不要随意改超参数范围（除非说明理由）

--------------------------------------
【行为要求】
--------------------------------------
- 先解释思路，再写代码
- 如果不确定，先提问，不要瞎改

--------------------------------------
【开发流程规则】
--------------------------------------

1. 每次修改核心算法：
   - 必须记录到 MEMORY.md（200字以内）
   - 必须说明修改目的

2. 修改前：
   - 必须查看 MEMORY.md
   - 避免重复实验

3. 修改后：
   - 运行训练
   - 运行测试
   - 生成结果

4. 代码要求：
   - 不允许伪实现
   - 优先简单高效方案